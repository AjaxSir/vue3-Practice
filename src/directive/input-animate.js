export default {
    beforeMount(el, bindings, vnode, preVnode) {
        console.log(el, bindings, vnode, preVnode)
    },
    mounted() {},
    beforeUpdate() {},
    updated() {},
    beforeUnmount() {},
    unmounted() {}
}